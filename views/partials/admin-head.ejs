<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="description" content="<%= meta.description %>"/>
  <title><%= pageTitle %> · CuzzyCrew Admin</title>
  <style>
    body.admin-body {
      margin: 0;
      background: #f8f4ec;
      color: #2d241a;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;
    }
    .admin-main,
    .admin-auth {
      max-width: 1200px;
      margin: 0 auto;
      padding: 18px;
    }
    .admin-panel,
    .admin-auth-card,
    .admin-page-header,
    .admin-kpi-card {
      background: #fffdf9;
      border: 0.9px solid #806549;
      padding: 14px;
      margin-bottom: 12px;
    }
  </style>
  <link rel="stylesheet" href="/admin.css?v=20260415e"/>
</head>
<body class="admin-body <%= activePage === 'login' ? 'admin-login-body' : '' %>">
<a class="skip-link" href="#main-content">Skip to main content</a>
